<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme - used after splash -->
    <style name="Theme.SimuSoul" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:statusBarColor">@color/background_light</item>
        <item name="android:navigationBarColor">@color/background_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.SimuSoul.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/background_light</item>
        <item name="postSplashScreenTheme">@style/Theme.SimuSoul</item>
    </style>
</resources>
